#include <algorithm>
#include <iostream>
#include <vector>

using namespace std;

int main()
{
    int a=72;
    int b= 60;
    int maxi = -1;
    for( int i= 1 ; i<= b ; i++)
    {
        if( a%i==0 && b%i==0)
        {
            maxi = max( maxi, i);
        }
    }
    cout<< maxi;
}

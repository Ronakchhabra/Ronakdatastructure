void reverse( string& str , int s, int e)
{
    if(s>e)return;
    swap(str[s++] , str[e--]);
    reverse(str , s, e);
    
}
string reverseString(string str)
{
    int s=0;
    int e= str.length()-1;
    reverse(str, s, e);
	//while(s<e)swap(str[s++] , str[e--]);
    return str;
}

#include <algorithm>
#include <iostream>
#include <bits/stdc++.h> 

using namespace std;

int power(int a , int b  )
{
    if(b==0)return 1;
    int result = power(a,b/2);
    if(b%2==0)return result * result;
   else return a *result * result;
 
   
    
    
   
}
int main()
{
    int a=6 ;
    int b=11;
  int ans= power(a , b );
    cout<< ans;
    return 0;}
